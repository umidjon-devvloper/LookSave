# export — Blender'dan chiqqan xom GLB

Optimizatsiyadan OLDINGI holat. Bu fayllar R2 ga yuklanmaydi.

```
nike_air-max-90_black_feet.glb
```

Nom qoidasi: `{brend}_{mahsulot-slug}_{rang}_{slot}.glb`
Faqat kichik lotin harf, `-` va `_`. Probel, kirill, `o'`/`g'` yo'q.

Xom fayl 5-20 MB bo'lishi normal — siqish keyingi bosqichda.
